import { StyleRules, Theme } from '@material-ui/core/styles';

export function styles(theme: Theme)  {
    return (
    {
		outerGrid: {
			padding: "24px"
		},
		formGroup: {
			padding: "16px"
		}
    } as StyleRules);
}